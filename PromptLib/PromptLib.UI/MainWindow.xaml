<Window
    x:Class="PromptLib.UI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PromptLib.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="MainWindow"
    Width="800"
    Height="450"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="75" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="PromptLib" />

        <StackPanel Grid.Row="1">
            <RadioButton
                Command="{Binding NavigateToLibraryCommand}"
                Content="Library"
                IsChecked="True" />
            <RadioButton Command="{Binding NavigateToBuildPromptCommand}" Content="Build" />
            <RadioButton Command="{Binding NavigateToSettingsCommand}" Content="Settings" />
        </StackPanel>

        <ContentControl
            Grid.Row="1"
            Grid.Column="1"
            Content="{Binding CurrentView}" />
    </Grid>
</Window>
